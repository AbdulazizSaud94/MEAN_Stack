<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <title>All Cats</title>
</head>

<body>
    <div class="container">
        <div class="row mt-3">
            <h3>All Cats</h3>
        </div>
        <% for (var cat of cats) { %>
        <div class="row mt-4">
            <a href="/cats/<%= cat.id %>">
                <h3><%= cat.name %></h3>
            </a>
        </div>
        <div class="row">
            <span> Favorite food:<%= cat.food %>, Age <%= cat.age %></span>

        </div>
        <% } %>

        <div class="row mt-4">
            <a href="/cats/new" class="btn btn-primary">Add new cat</a>
        </div>
    </div>
</body>

</html>